using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefaultModelSizes : MonoBehaviour {

    public Vector3 TigerDefaultSize = new Vector3(0.5f, 0.5f, 0.5f);
    public Vector3 TreeDefaultSize = new Vector3(0.1f, 0.1f, 0.1f);
    public Vector3 CarDefaultSize = new Vector3(0.1f, 0.1f, 0.1f);
    public Vector3 PlaneDefaultSize = new Vector3(0.00017f, 0.00017f, 0.00017f);


    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
